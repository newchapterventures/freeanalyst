# 私有资产——永不进 git
root/
index/
private/
cache/
out/
audits/*.jsonl

# 评测跑分产物
#
# **模型质量评测的原始结果要进仓库** —— 它是对外主张的证据
# （README 里写了「8.9GB 的 gemma3 打败 18GB 的 qwen3」，
# 别人应该能翻到原始答案自己核）。
# 但那些是临时对比、看一眼就删的，不进。
bench/results-baseline.json
bench/results-*-rescored.json

# 常见的机密材料类型（防止误提交）
#
# **例外**：corpus/test-*.pdf 是合成的测试材料（由同名 .html 用浏览器渲染），
# 不含任何真实交易信息，测试要用，必须进仓库。
*.pdf
*.docx
*.xlsx
*.pptx
*.csv
!corpus/test-*.pdf

# 系统与缓存
__pycache__/
.DS_Store
*.pyc
.env
.venv/
