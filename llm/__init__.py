"""模型层 —— 适配本机装的开源大模型。

    backends    本机模型运行时的适配（ollama / 任何 OpenAI 兼容的本地服务）
    gate        质量门槛（三类致命错误一个都不能犯）

## 一条贯穿这一层的前提

**大模型不是必需项。** 这个工具里提取三张表、判断口径、勾稽校验、
估值计算全是纯代码 —— 没装模型照样能跑出估值结论。

大模型只用在「读了材料回答问题」和「理解业务描述提行业建议」这两处，
都是增强。所以：

    没检测到任何模型运行时 → **不是错误**，`pick()` 返回 None，功能降级
    检测到了但没过质量门槛 → 也不自动启用，得让用户知道

## 所有请求只走回环

`guard.guarded_request()` **只放行 127.0.0.1**。
所以「模型跑在本机、数据不出网」不是一句承诺，是代码里挡着的 ——
而且用户能自己跑 `tests/test_egress_guard.py` 验证。
"""

from . import backends, gate

__all__ = ["backends", "gate"]
