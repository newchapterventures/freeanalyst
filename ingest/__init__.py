"""输入层 —— 把各种格式的材料变成可检索的文本。

| 格式 | 状态 | 依赖 |
|---|---|---|
| `.txt` / `.md` | ✅ | 零依赖 |
| **`.pdf`** | ✅ 文字 + 表格 | 可选：pdfplumber |
| `.docx` / `.xlsx` | ⬜ 未做 | — |

**唯一一个可选依赖是 pdfplumber**，而且只用在这里。
没装它，项目的其余部分照常工作 —— 这是有意的：
零依赖的核心 + 可选的增强。

## 这一层存在的理由

真实的尽调材料是 PDF，而 PDF 抽出来的中文经常不是你以为的那些字。
详见 `normalize.py` 里那段说明 —— **不归一化会让检索静默失配**。
"""

from . import normalize, pdf

__all__ = ["normalize", "pdf"]
