# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.0] — 2026-09-15

首个版本。**核心不是功能，是可验证的隔离。**

### 三个承诺，三种证明方式

| 承诺 | 证明方式 |
|---|---|
| 代码全开源 | 全部代码在仓库里，任何人可审计 |
| 数据零出境 | 全项目唯一网络出口是 `guard.py`，只放行 localhost，每次调用留痕 |
| 结论可追溯 | 每条结论强制带原文出处；材料里没有的一律写"未提供" |

### 加入

- **`guard.py`** —— 出网闸门。全项目唯一的网络出口，非回环地址一律拦截并记入审计日志
- **`retrieval.py`** —— 本地检索。BM25 + 中文字符二元组，零第三方依赖，不需要下载词典
- **`freeanalyst.py`** —— CLI：`ingest` / `ask` / `audit` / `doctor`
- **`root-templates/`** —— 12 份投资判断框架空白模板（基金身份、赛道观点、红旗清单、
  筛选阈值、条款立场、证据规则、备忘录、失败条款库、交易日志、尽调清单、投后指标、管理层评分）
- **`tests/test_egress_guard.py`** —— 出网闸门测试，5 项
- **`bench/model_quality.py`** —— 模型质量评测，4 个用例

### 实测数据

| 项目 | 结果 |
|---|---|
| 出网闸门测试 | **5/5 通过** —— OpenAI / Anthropic / Google / DeepSeek / 阿里百炼的地址全部拦截；`example.com` / `1.1.1.1` / `192.168.1.1` / `ftp://` 同样拦截 |
| 审计日志 | 总调用 3 · 放行 3（全回环）· 拦截 0 · **外部主机 无** |
| 模型质量（`qwen2.5-coder:7b`） | **1/4，未达门槛** |

模型质量明细：

```
[失败] 主体识别      ↳ 把实控人的个人回购义务说成公司的财务压力
[失败] 跨文档冲突    ↳ 只引用 CIM，未引用访谈纪要 —— 未做跨文档比对
[通过] 格式遵循
[失败] 拒答幻觉      ↳ 结论节编造了材料中不存在的「已闭环」
```

### 关于评测本身

评测的第一版用关键词匹配，**让同一个 7B 模型拿到了 4/4「够格进生产」**——
因为它在「材料缺口」节里写了"材料未提供"（格式要求的），就从「结论」节里
编造内容的行为中蒙混过关。

第二版改成按节解析 + 来源覆盖 + 语义模式，同一模型得分降到 1/4。

**换模型之前，先用一个已知不合格的模型跑一遍评测。**
如果它全过，说明评测失效了——这比模型不合格更严重。

### 已知限制

- 只支持 `.txt` / `.md`，PDF 与 Word 未支持
- 检索是纯词频（BM25），尚无向量召回
- 未提供 MCP server，接入 Claude Code / Cursor 等宿主需自行封装
- **24GB 内存的机器只能跑 7B–12B 级模型，而 7B 未达质量门槛** ——
  可用的硬件组合与模型档位尚未确定

### 说明

本版本为技术工具，不含任何形式的投资建议、业绩承诺或募资信息。
